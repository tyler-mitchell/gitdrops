import { block, For, macro } from "million/react";

const TestArray = block(() => {
  return (
    <div>
      {/* <For each={["a", "b", "c"]}>{(e, i) => <div key={i}>{e}</div>}</For> */}
      Fuck FUCK
    </div>
  );
});

export default TestArray;
